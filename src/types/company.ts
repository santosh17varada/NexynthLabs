export type CompanyVisionPillar = {
  id: string;
  title: string;
  description: string;
};

export type LeadershipMember = {
  id: string;
  name: string;
  role: string;
  bio: string;
  isPlaceholder: boolean;
};

export type CompanyPrinciple = {
  id: string;
  title: string;
  description: string;
};
